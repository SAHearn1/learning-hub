# Workflows

Exported n8n workflow JSON files.

## Usage

Import these JSON files into your n8n instance to deploy the AI work agent workflows.

## Naming Convention

Files should follow the pattern: `<agent-name>-<workflow-description>.json`

Example: `research-agent-web-search.json`
